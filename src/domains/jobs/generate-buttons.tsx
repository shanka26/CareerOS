"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{Button}from"@/shared/ui/button";export function GenerateButtons({jobId,cover=false}:{jobId:string;cover?:boolean}){const[pending,setPending]=useState(false);const[error,setError]=useState<string>();const router=useRouter();return <div><Button disabled={pending} onClick={async()=>{setPending(true);const kind=cover?"cover-letter":"resume";const response=await fetch(`/api/jobs/${jobId}/generate-${kind}`,{method:"POST"});const result=(await response.json())as{documentId?:string;error?:string};if(response.ok&&result.documentId)router.push(`/dashboard/documents/${result.documentId}`);else{setError(result.error);setPending(false)}}}>{pending?"Generating…":cover?"Compose cover letter":"Compose verified resume"}</Button>{error?<p className="mt-2 text-sm text-red-700">{error}</p>:null}</div>}
