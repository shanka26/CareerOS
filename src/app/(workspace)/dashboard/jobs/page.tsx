import Link from "next/link"; import { requireSession } from "@/domains/settings/auth/session"; import { JobImportForm } from "@/domains/jobs/job-import-form"; import { prisma } from "@/shared/db/prisma"; import { Card } from "@/shared/ui/card";
export default async function JobsPage(){const session=await requireSession();const jobs=await prisma.jobPosting.findMany({where:{ownerId:session.user.id},orderBy:{importedAt:"desc"}});return <main className="mx-auto max-w-7xl px-5 py-10 sm:px-8"><h1 className="font-[family-name:var(--font-display)] text-5xl">Job workspaces</h1><div className="mt-8 grid gap-6 lg:grid-cols-[.9fr_1.1fr]"><Card className="p-6"><h2 className="mb-5 text-xl font-bold">Import a job</h2><JobImportForm/></Card><div className="grid content-start gap-3">{jobs.map(job=><Link key={job.id} href={`/dashboard/jobs/${job.id}`}><Card className="p-5"><div className="flex justify-between"><div><h2 className="font-bold">{job.title}</h2><p className="text-sm text-[var(--muted)]">{job.companyName}</p></div><strong>{job.matchScore??0}%</strong></div></Card></Link>)}</div></div></main>}
